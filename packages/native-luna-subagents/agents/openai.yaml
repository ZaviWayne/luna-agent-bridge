interface:
  display_name: "Native Luna Subagents"
  short_description: "Default to native Luna subagents"
  default_prompt: "Coordinate independent work with Codex native subagents and keep the external bridge opt-in."

policy:
  allow_implicit_invocation: true
